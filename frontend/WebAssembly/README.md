In order to install the C dependencies, run the following in the shell:

```
./install_dependencies.sh
```

To create the WASM module, run the following

```
make
```

To clean the environment, run:

```
make clean
```