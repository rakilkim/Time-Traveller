In order to install the Rust dependencies, run the following in the shell:

```
./manage-rust.sh
```

To create the WASM module, run the following

```
npm run build:rs
```

To clean the environment, run:

```
npm run clean
```